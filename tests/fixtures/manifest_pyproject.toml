[project]
name = "flask"
version = "3.1.0"
description = "A microframework"
dependencies = [
    "Werkzeug>=3.1",
    "Jinja2>=3.1.2",
    "itsdangerous>=2.2",
    "click>=8.1.3",
]

[project.scripts]
flask = "flask.cli:main"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"
