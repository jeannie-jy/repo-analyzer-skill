{
  "entrypoints": [
    {"path": "src/click/__init__.py", "kind": "library_api", "note": "Pure library: the import surface is the entry. The deterministic library-package-root heuristic (since 2026-08-24) emits this candidate; the LLM phase confirms it and raises its confidence from the sampled __init__.py."}
  ],
  "expected_paths": [
    "src/click/__init__.py",
    "src/click/core.py",
    "src/click/decorators.py",
    "src/click/parser.py",
    "src/click/types.py",
    "pyproject.toml",
    "README.md",
    "tests",
    "docs",
    "examples"
  ],
  "note": "Library-only case: the import surface is the only entry. The deterministic library-package-root heuristic detects it (kind library_api), so entrypoint F1 is measurable — measured 1.00 on 2026-08-24 (live main)."
}
