# Case: charmbracelet/gum

A small Go CLI with exactly two real entries (the Go main package and
the container build). Represents the best-case shape for the entrypoint
heuristics, balancing the flask (medium, noisy) and click (library,
invisible) cases.
